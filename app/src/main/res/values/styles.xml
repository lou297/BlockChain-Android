<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/textBlue</item>
        <item name="colorPrimaryDark">@color/textBlue</item>
        <item name="colorAccent">@color/borderBlue</item>
    </style>

    <style name="TabTextAppearance" parent="TextAppearance.AppCompat">
        <!-- Customize your theme here. -->
        <item name="android:fontFamily">@font/nanum_pen</item>
        <item name="android:textSize">22dp</item>
        <item name="android:textColor">@color/textBlue</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
